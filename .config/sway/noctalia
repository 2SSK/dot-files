# ── Noctalia v4 (Quickshell) Colors ───────────────────────────────────
# set $primary            #7aa2f7
# set $on_primary         #16161e
# set $secondary          #bb9af7
# set $tertiary           #9ece6a
# set $error              #f7768e
# set $surface            #1a1b26
# set $on_surface         #c0caf5
# set $on_surface_variant #9aa5ce
# set $outline            #586691

# ## Window Colours
# # class                 border    backgr.   text                indicator  child_border
# client.focused          $primary  $surface  $on_surface         $primary   $primary
# client.focused_inactive $outline  $surface  $on_surface_variant $outline   $outline
# client.unfocused        $outline  $surface  $on_surface_variant $outline   $outline
# client.urgent           $error    $surface  $on_surface         $error     $error
# client.placeholder      $surface  $surface  $on_surface_variant $surface   $surface
# client.background       $surface

# ── Noctalia v5 Keybinds ─────────────────────────────────────────────
# Launch Noctalia (if not autostarted by compositor)
# exec noctalia

# Shell IPC — panels & toggles
# bindsym $mod+space   exec noctalia msg launcher toggle
# bindsym $mod+Tab     exec noctalia msg window-switcher toggle
# bindsym $mod+Escape  exec noctalia msg control-center toggle
# bindsym Print         exec noctalia msg screenshot-region
# bindsym Shift+Print   exec noctalia msg screenshot-fullscreen

# Media & clipboard
# bindsym $mod+v       exec noctalia msg clipboard-clear
# bindsym XF86Tools    exec noctalia msg clipboard-toggle

# System controls
# bindsym XF86AudioRaiseVolume  exec noctalia msg volume-up 5
# bindsym XF86AudioLowerVolume  exec noctalia msg volume-down 5
# bindsym XF86AudioMute         exec noctalia msg volume-mute
# bindsym XF86MonBrightnessUp   exec noctalia msg brightness-up 5
# bindsym XF86MonBrightnessDown exec noctalia msg brightness-down 5

# Session
# bindsym Ctrl+Alt+l   exec noctalia msg session lock
# bindsym Ctrl+Alt+Del exec noctalia msg session logout
